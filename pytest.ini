[pytest]
testpaths = tests
addopts = -ra
filterwarnings =
    error
    # huggingface_hub closes cached files from a finaliser; the timing of that
    # is not ours to fix and it is not a defect in anything we wrote.
    ignore::pytest.PytestUnraisableExceptionWarning
